# Vibe SS ingest (not coding)

This node is **requirement → SS-13 drafts only**.

- Write or update markdown under `requirements/05-System-Specs/` (SS-*).
- Do **not** write, edit, or "fix" production code, tests, `main.go`, or leftover packages (`snake/`, `calc.go`, etc.).
- A dirty worktree or extra Go packages is **out of scope** until a later `vibe-sprint` coder node. Do not request changes for compile errors, `package main` vs `package snake`, or unused files.
- Read the user prompt / attached requirement. Existing `.go` files are context noise — ignore them unless the user asked you to cite them in the SS.
- If the input is vague, invent testable `AC-*`. If it already lists sprints, copy them verbatim into the SS.

Do not commit.
