---
name: kmm-conventions
description: Kotlin Multiplatform (KMM) conventions for FlowPilot. Placeholder — expand with shared-module rules.
version: 2
---

# Kotlin Multiplatform (KMM) conventions

Starter skill installed for **Kotlin Multiplatform** projects alongside the common FlowPilot skill pack, plus the Android and iOS skills.

Add KMM-specific conventions here — `commonMain`/`androidMain`/`iosMain` source-set layout, `expect`/`actual` rules, shared-module structure, and testing rules the AI should follow.

## TODO

- Replace this placeholder with real Kotlin Multiplatform guidance.
