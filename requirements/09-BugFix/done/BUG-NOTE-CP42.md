# 1. [FIXED] P1 - flowRef Review Loop can race the hub’s first turn, so the hub may still code instead of waiting.

[interactive_service.go (line 2560)](/C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:2560) starts startResolvedFlow in a goroutine, then [drains `pendingAgentContext` (line 2590)](/C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:2590) for the current provider turn. The wait notice is appended later in [notifyHubFlowStarted (line 102)](/C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:102), so the first hub turn usually will not see it. Current tests only check that a child appears async or that synchronous startResolvedFlow appends a note, not that the live first hub prompt receives the note. This can duplicate work: parent hub + spawned coder both act on the same user request.

**Fix**: confirmed real. `startTurn` now prepends the `flow-start-wait.md` notice synchronously to `in.Prompt` before dispatching `startResolvedFlow`'s goroutine, so the hub's own first-turn prompt always carries the wait notice regardless of goroutine scheduling; the unmodified original prompt is still passed to `startResolvedFlow` for the spawned child. See [interactive_service.go (line 2559)](/C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:2559). Covered by new test `TestStartTurnWithFlowRefPrependsWaitNoticeToHubsOwnFirstTurn` in [flow_executor_test.go](/C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor_test.go), which asserts the wait notice is present in the parent run's own provider-observed first-turn prompt (not just in a later pending-context note).

# 2. [FIXED] P1 - Built-in mirror sync is likely broken against real Supabase/PostgREST.

Migration creates a partial unique index on (pack_id, pack_flow_id) where is_builtin = true in [20260701090000_add_flow_engine_attrs_to_workflows.sql (line 39)](/C:/working/flowpilot/supabase/migrations/20260701090000_add_flow_engine_attrs_to_workflows.sql:39), but the store upserts with on_conflict=pack_id,pack_flow_id in [supabase_workflow_flow_store.go (line 296)](/C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:296). Postgres/PostgREST generally cannot infer a partial index from that conflict target, so startup mirror sync at [root.go (line 147)](/C:/working/flowpilot/apps/local-runner/internal/cli/root.go:147) can fail in real DBs. The current test only asserts the URL string, not DB-valid upsert behavior.

**Fix**: confirmed real. Since this migration was never applied to any live database (new/uncommitted file), dropped the `where is_builtin = true` predicate entirely rather than special-casing PostgREST's `on_conflict` param — a plain `unique index on workflows(pack_id, pack_flow_id)` needs no predicate because non-builtin rows always leave both columns `NULL` (see `Upsert`'s builtin/non-builtin branches), and a standard btree unique index never treats two `NULL`s as conflicting. Added `TestWorkflowsPackFlowUniqueIndexIsNotPartial` (`workflow_flow_migration_sql_test.go`) as a regression guard, since no live Postgres is available in this environment to exercise the actual `INSERT ... ON CONFLICT` failure directly — it asserts the migration's `CREATE UNIQUE INDEX` statement for `workflows_pack_flow_uidx` has no `WHERE` clause.

# 3. [FIXED] P2 - Flow node edits in Settings do not enable Save.

normalizeWorkflowSnapshot omits nodeId, behaviorId, agentRef, dependsOn, joinMode, and cohort in [WorkflowsSettings.tsx (line 98)](/C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:98), while those fields are edited at [WorkflowsSettings.tsx (line 758)](/C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:758). Because workflowDirty depends on that snapshot and Save is disabled at [WorkflowsSettings.tsx (line 1480)](/C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:1480), users can edit CP-42 node fields but cannot save unless they also change some older field.

**Fix**: confirmed real. `normalizeWorkflowSnapshot`'s per-step snapshot now includes `nodeId`/`behaviorId`/`agentRef`/`dependsOn`/`joinMode`/`cohort` (and `promptTemplateRef`/`contextRef`, the same class of field, sharing the same form, not named in this bug but equally missing), so `workflowDirty` correctly detects an edit to any of them and Save enables. `dependsOn` is sorted before stringifying, matching the existing `requiredMcps`/`requiredSkills` pattern in `normalizeStepSnapshot`, so array-order-only differences don't produce a false-dirty flip. Verified via `tsc` (no new type errors) and the full `tests/phase1` suite (42 passed, 3 pre-existing failures in unrelated files unchanged).

# 4. [FIXED] P1 - activeFlowEdges is set after the entry child is spawned, so auto-advance can miss the flow topology.

[flow_executor.go (line 46)](/C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:46) calls spawnChildRun, and only later sets rs.activeFlowEdges / activeFlowNodes at [flow_executor.go (line 69)](/C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:69). But spawnChildRun fires the child’s first turn asynchronously before returning in [interactive_service.go (line 1938)](/C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:1938). If the coder child completes fast, advanceOrNotifyHub can run while the parent has no edges yet, so tryAdvanceFlowFromNode falls back instead of spawning reviewers deterministically.

**Fix**: confirmed real. `startResolvedFlow` now sets `rs.activeFlowEdges`/`rs.activeFlowNodes` immediately after resolving the flow definition, before the entry-node spawn loop runs at all — see [flow_executor.go (line 52)](/C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:52). This removes the timing dependency entirely rather than narrowing the race window. Existing test `TestCoderCompletionAutoSpawnsReviewerCohort` continues to pass and now exercises the corrected ordering (it uses a fake adapter that completes the coder's turn essentially immediately, which is exactly the fast-completion scenario this bug describes).

# 5. [FIXED] P2 - AgentPack validation does not enforce unique flow IDs.

Task-173 explicitly requires duplicate flow IDs to fail, but [pack.go (line 326)](/C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:326) names the map seenFlowIDs while actually keying it by flow.Path at [pack.go (line 336)](/C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:336). Two files can both declare id: review-loop, and the resolver/picker will become ambiguous.

**Fix**: confirmed real — `ValidateManifestFS`'s check was trivially always true (manifest entry paths are inherently unique per list item) and never looked at the flow files' own content. Added a cross-file `def.ID` uniqueness check in `LoadPackFS` (`pack.go`), right after each flow file is actually loaded and parsed, where every flow's real content-level ID is available. Covered by new test `TestLoadPackFSRejectsDuplicateFlowContentID` (`pack_test.go`), using a two-file fixture where both flows declare `id: shared-id`.

# 6. [FIXED] P2 - Context render template refs are parsed incorrectly and not validated.

The context YAML declares:

[flow-context-package.yaml (line 16)](/C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/contexts/flow-context-package.yaml:16) render.promptTemplate. But [pack.go (line 699)](/C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:699) reads render as a scalar string, so it becomes a Go stringified map rather than prompts/flow-context-handoff.md. Task-173 T-8 also requires prompt/context refs to resolve to manifest entries; tests do not cover this.

**Fix**: confirmed real (a latent bug — nothing reads `ContextArtifact.RenderedTemplate` at runtime yet, same class as #10, but every pack load already produced garbage in that field). `contextArtifactFromMap` (`pack.go`) now reads the nested `render.promptTemplate` map field instead of coercing the whole `render` map via `fmt.Sprint`. Also added the T-8 half: `LoadPackFS` now cross-checks every context's `RenderedTemplate` and every flow node's `PromptTemplate` against `manifest.Prompts`, rejecting a pack where a referenced prompt path isn't actually declared in the manifest. Verified the real embedded pack still passes this stricter check (full `go build`/`go test ./internal/runner/...` clean after the change — nothing in the shipped `flow-pack` violates it). Covered by two new tests: `TestContextArtifactParsesNestedRenderPromptTemplate` (parsing) and `TestLoadPackFSRejectsPromptRefNotDeclaredInManifest` (T-8 cross-reference check).

# 7. [FIXED] P2 - The live workflow step bridge drops CP-42 behavior_id / node_id.

The migration stores node identity on workflow_steps at [migration (line 52)](/C:/working/flowpilot/supabase/migrations/20260701090000_add_flow_engine_attrs_to_workflows.sql:52), but [SupabaseWorkflowStore.LoadRunSteps (line 64)](/C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_store.go:64) only selects run-step step_type plus requires_approval. RuntimeWorkflowStep never receives behavior_id, so runtime helpers like isCodingStepType still classify from step_type only. That leaves UI-authored generic flow behavior disconnected from the normal run-step path.

**Fix**: confirmed real — worse than "disconnected," `NormalizeBehaviorID` doesn't even recognize the CP-42 generic dispatch-category step_type values (e.g. `"flow-agent-delegate"`) as aliases at all, so a UI-authored generic flow's coding/plan steps were completely unclassifiable, not just inconsistently classified. Added `BehaviorID` to `RuntimeWorkflowStep` (`workflow_state_machine.go`); `LoadRunSteps`'s embedded `workflow_steps(...)` select now also requests `behavior_id` and populates it. `isCodingStepType`/`isPlanStepType` (`flow_context_handoff.go`) now take `(behaviorID, stepType string)` and classify via a new `classifyStepBehavior` helper that prefers `behaviorID` when set, falling back to `stepType` (preserving the legacy `"coding"`/`"plan"` alias behavior for steps predating CP-42). All three call sites updated to pass both fields. Covered by new tests `TestGenericFlowStepTypeClassifiesByBehaviorID` (classification logic) and an extended `TestSupabaseStoreLoadRunStepsShaping` (asserts the embed select and decode include `behavior_id`).

# 8. [FIXED] P3 - Built-in workflows are visually editable even though Save/Delete are hidden.

The detail copy says built-ins cannot be edited, and Save is hidden, but fields like Name/Description/Model and all step/node inputs still have active onChange handlers and no disabled binding. Example: node inputs around [WorkflowsSettings.tsx (line 752)](/C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:752), workflow fields around [WorkflowsSettings.tsx (line 1492)](/C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:1492). Users can type changes that cannot be saved, which undercuts the read-only built-in UX.

**Fix**: confirmed real. Added `workflowDetailReadOnly` (`selectedWorkflow?.editable === false`) and bound `disabled={workflowDetailReadOnly}` to every workflow-level detail field (Owner project, Name, Description, Model override, Reasoning effort, YOLO mode). The existing per-step `readOnly` constant in `renderWorkflowStepsEditor` was already correctly bound to the Add/Move/Remove buttons but not to the step fields themselves — added `disabled={readOnly}` to Model override, Reasoning, Enabled, Requires approval, and all six CP-42 flow-node inputs (Node ID/Behavior ID/Agent ref/Depends on/Join mode/Cohort). Verified via `tsc` (no new type errors) and the full `tests/phase1` suite (42 passed, 3 pre-existing failures in unrelated files unchanged). Not verified in a live browser preview: desktop-flowpilot is an Electron app, not a plain web dev server, and did not render in this session's browser-preview harness (no running server after `preview_start`; a screenshot attempt timed out) — confirmed via `tsc`/tests and direct code review instead.

# 9. [FIXED] [P1] flowRef executor không chạy được flow có entry node inline.
 startResolvedFlow chỉ tìm entry agent.delegate và log “nothing to start” nếu không có ([flow_executor.go (line 37)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:37), [flow_executor.go (line 41)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:41)). rag-harness lại bắt đầu bằng context node run: inline / behavior: context.produce ([rag-harness.yaml (line 23)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:23)). Kết quả: Review Loop may chạy, nhưng Flow Mode/mirrored rag-harness selection sẽ inert. Chat baseline có vẻ vẫn còn qua legacy injectFlowContextIfCoding, nhưng đường generic flow thì chưa xử lý inline entry.

**Fix**: confirmed real. Added `startInlineEntryChain` (`flow_executor.go`): when `entryDelegateNodes` finds nothing, checks for the shape either built-in flow actually declares — exactly one no-dependency inline-scope entry node with a single forward-"done" hop to an `agent.delegate` node. Runs the inline node synchronously via `DefaultBehaviorRegistry().Dispatch` (no provider call, matching `BehaviorScopeInline`'s contract), renders its `FlowContextPackage` output into the delegate node's prompt via the existing `renderFlowContextPrompt`/`ComposeFlowCodingPrompt` path, and spawns the delegate node exactly like the delegate-entry path does — including setting `activeFlowEdges`/`activeFlowNodes` before spawning (same BUG#4 ordering). Longer inline chains or multiple entry nodes are out of scope (neither built-in flow declares one) and fall through to the pre-existing "nothing to start" log line rather than guessing. Covered by new test `TestStartResolvedFlowStartsInlineEntryFlow` in `flow_executor_test.go`, which starts `rag-harness` end to end and asserts the `implement` delegate node is spawned.

# 10. [FIXED] [P1] Supabase mirror làm mất context graph của pack. 
rag-harness.yaml có flow-level contexts, node inputs, outputs ([rag-harness.yaml (line 19)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:19), [rag-harness.yaml (line 27)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:27), [rag-harness.yaml (line 34)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:34)), nhưng SupabaseWorkflowFlowStore select/reconstruct chỉ round-trip step/node fields và prompt_template_ref ([supabase_workflow_flow_store.go (line 116)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:116), [supabase_workflow_flow_store.go (line 118)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:118)). replaceSteps cũng không persist Inputs/Outputs hay flow Contexts ([supabase_workflow_flow_store.go (line 342)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:342)). Khi resolver ưu tiên mirror row, normalized definition không còn đủ data để chạy context/package behavior.

**Fix**: confirmed real (a latent data-loss bug: nothing at runtime reads `Definition.Contexts`/`FlowNode.Inputs`/`Outputs` yet — confirmed via search — so it doesn't break current behavior, but every mirror-sync round-trip already silently drops this data, which would corrupt any future context/package-behavior wiring). Added `contexts_json` to `workflows` and `inputs_json`/`outputs_json` to `workflow_steps` in the migration (new/uncommitted file, edited in place); wired `recordFromWorkflowRow` to decode both into `agentpack.FlowDefinition.Contexts`/`FlowNode.Inputs`/`Outputs`, and `Upsert`/`replaceSteps` to encode them back (`contextsPayload`/`nonNilStringMap` helpers). Covered by two new tests in `supabase_workflow_flow_store_test.go`: `TestSupabaseWorkflowFlowStoreGetByRefDecodesInputsOutputsAndContexts` (read side) and `TestSupabaseWorkflowFlowStoreUpsertSendsContextsAndNodeInputsOutputs` (write side).

# 11. [FIXED] [P2] Declared tool face đang là pack data nhưng runtime chưa dùng pack data. 
YAML khai báo statusMap/payloadMap ([submit-review-outcome.yaml (line 33)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/tools/submit-review-outcome.yaml:33)), parser có đọc ToolFace.StatusMap ([pack.go (line 677)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:677)), nhưng runtime vẫn hardcode reviewOutcomeFace() và reviewOutcomeToFlowControl() ([agent_orchestrator.go (line 567)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:567), [agent_orchestrator.go (line 693)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:693)). CP-42 P-5 yêu cầu declared faces become pack data, nên đây vẫn là gap.

**Fix**: confirmed real. Added `agentpack.LoadBuiltinToolFace(id)` (mirrors the existing `LoadBuiltinPrompt` pattern) and changed `reviewOutcomeFace()` (`agent_orchestrator.go`) to build its `FlowControlFace{Tool, Map}` from the pack's own declared `submit-review-outcome.yaml` face (`ID`/`StatusMap`) instead of an independent hardcoded literal — the two values were already identical, so this closes the "parsed but ignored" gap without changing any current behavior. Falls back to the pre-existing literal only if the pack can't be loaded or has no such face, so a packaging problem degrades gracefully. `reviewOutcomeToFlowControl` needed no changes — it already calls `reviewOutcomeFace()` rather than duplicating the map itself. `payloadMap` remains unused (nothing in the live path reads `ToolFace.PayloadMap` yet); left as a separate, smaller follow-up rather than folded into this change. Covered by new test `TestReviewOutcomeFaceReadsFromPackDeclaredStatusMap` in `agent_orchestrator_test.go`, which asserts `reviewOutcomeFace()`'s `Tool`/`Map` equal `agentpack.LoadBuiltinToolFace("submit_review_outcome")`'s own `ID`/`StatusMap`.

# 12. [FIXED] [P2] CloneBuiltin của resolver không tương thích schema Supabase hiện tại. 
Migration định nghĩa cloned_from uuid references workflows(id) ([migration (line 19)](C:/working/flowpilot/supabase/migrations/20260701090000_add_flow_engine_attrs_to_workflows.sql:19)), nhưng FlowDefinitionResolver.CloneBuiltin set ClonedFrom: source.FlowRef ([flow_definition_resolver.go (line 197)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_definition_resolver.go:197)). Với built-in mirror, source.FlowRef là stable ref kiểu flowpilot-core-flow-pack/review-loop, không phải UUID. UI clone hiện đi qua SupabaseAdminRepository.cloneWorkflow và dùng source.id, nên path desktop chính có vẻ không dính, nhưng resolver API/store path sẽ fail nếu được dùng lại.

**Fix**: confirmed real (dead-but-reachable code path today — the live desktop UI's own clone path avoids this entirely, as the note observes). `CloneBuiltin` (`flow_definition_resolver.go`) now only sets `ClonedFrom` when `source.FlowRef` actually `looksLikeUUID` (reusing the existing helper from `supabase_workflow_flow_store.go`); a built-in source (whose `FlowRef` is always the stable `"packId/flowId"` ref, never a UUID — `FlowDefinitionStore`'s interface has no way to recover the underlying mirror row's real id) leaves `ClonedFrom` unset instead of inserting a value that would violate the FK constraint against a real database. This is a known limitation, not silently wrong data: a built-in-sourced clone simply has no `cloned_from` provenance recorded today. Updated the pre-existing `TestCloneBuiltinCreatesEditableUserCopy` (which had encoded the old, broken expectation) and added new test `TestCloneBuiltinFromUUIDSourcePreservesClonedFrom`, which seeds the fake store with a UUID-shaped `FlowRef` directly (the shape a user-owned/already-cloned flow would have) to prove the `looksLikeUUID` branch itself works per the store interface's contract.

# 13. [FIXED] [P1] Reviewer auto-spawn prompt có thể cho phép từng reviewer tự advance flow trước khi join xong. 
tryAdvanceFlowFromNode spawn reviewer cohort với prompt “report your findings via the flow's declared control tool” ([flow_executor.go (line 166)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:166)). Trong khi submit_review_outcome được đăng ký như dynamic tool cho mọi Codex turn ([codex_adapter.go (line 151)](C:/working/flowpilot/apps/local-runner/internal/runner/codex_adapter.go:151)), và child call sẽ route flow_control lên parent ([interactive_service.go (line 1698)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:1698)). Nếu reviewer làm theo prompt, nó có thể trigger continue/done/escalate trước khi cohort barrier/hub synthesis xử lý đủ reviewers, trái Task-091 T-3 “reviewer only contributes to cohort note”.

**Fix**: confirmed real, and traced to my own earlier Task-180/CA-163 change (not a pre-existing defect): `tryAdvanceFlowFromNode`'s auto-spawn prompt told the reviewer to call the control tool directly, unlike every other reviewer-spawn path in this codebase (the manually-spawned E2E tests just say "review the coder output" — no tool instruction), and unlike the built-in `reviewer` agent persona itself (`agent_catalog.go`), which only asks for `APPROVED`/`CHANGES-REQUESTED` text — only the `synthesizer` persona is instructed to call `submit_review_outcome`, and only after consolidating the cohort join note. `turnBridge.SubmitFlowControl` (`interactive_service.go`) routes a child's flow_control call straight to `applyFlowControl(parentRunID, ...)` with no cohort-join gate at all, so the reviewer's prompt alone was the only thing standing between a normal review and a bypassed join barrier. Reworded the auto-spawn prompt to match the established convention — review and report findings as the final message, no tool call. Covered by new test `TestCoderCompletionAutoSpawnedReviewerPromptDoesNotInstructFlowControlCall` in `flow_executor_test.go`, which asserts the auto-spawned reviewer's prompt contains none of "control tool", "flow_control", or "submit_review_outcome".

# 14. [FIXED] [P1] Save Workflow có thể xoá policy/edges hiện có. 
UI saveWorkflow chỉ gửi base fields + steps, không gửi policyCap/policyOnCap/.../edges ([WorkflowsSettings.tsx (line 433)](C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:433)). Repository lại upsert policy_* thành null và edges_json thành [] khi fields không có ([supabaseAdminRepository.ts (line 553)](C:/working/flowpilot/packages/flowpilot-client-core/src/data/supabaseAdminRepository.ts:553), [supabaseAdminRepository.ts (line 557)](C:/working/flowpilot/packages/flowpilot-client-core/src/data/supabaseAdminRepository.ts:557)). Chỉ đổi tên workflow clone cũng có thể wipe graph/policy data.

**Fix**: confirmed real — the repository has no notion of "field omitted, leave unchanged"; `saveWorkflow`'s upsert always writes `policyCap ?? null`/`edges ?? []`, so any save (even just a rename) wiped these fields on the target row. `WorkflowDraft` (`WorkflowsSettings.tsx`) gained pass-through-only fields (`policyCap`/`policyOnCap`/`policyExtendBy`/`policyExtendMax`/`edges` — no UI control edits them, matching the fact that neither this bug nor #22/#3 found any editing surface for these fields today), populated from the loaded `Workflow` in `mapWorkflowToDraft` (defaulting to `null`/`[]` only for a genuinely brand-new workflow in `createEmptyWorkflowDraft`). Both `saveWorkflow`/`saveNewWorkflow` call sites now send these values back unchanged, so an edit to any other field no longer collaterally wipes policy/edges. Verified via `tsc` (no new type errors against the real compiler) and the full `tests/phase1` suite (42 passed, 3 pre-existing failures in unrelated files unchanged) — no dedicated unit test was added since this component has no existing test harness to extend and the fix is a straightforward pass-through wiring, not new logic.

# 15. [FIXED] [P2] Missing/stale built-in mirror không được recreate ở run-start path.
 CP-42 yêu cầu selecting built-in khi mirror missing phải trigger sync/recreate before run start. Nhưng startResolvedFlow chỉ ResolveFlowRef ([flow_executor.go (line 33)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:33)); resolver miss store thì fallback embedded pack ([flow_definition_resolver.go (line 108)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_definition_resolver.go:108), [flow_definition_resolver.go (line 130)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_definition_resolver.go:130)). Sync chỉ chạy best-effort ở startup ([root.go (line 147)](C:/working/flowpilot/apps/local-runner/internal/cli/root.go:147)), nên delete mirror rồi select flow sẽ không recreate row.

**Fix**: confirmed real. `FlowDefinitionResolver.ResolveBuiltin` (`flow_definition_resolver.go`) now attempts to recreate a missing mirror row via `store.Upsert` right after falling back to the embedded pack, returning the freshly-mirrored record (Source `supabase_builtin_mirror`) on success. Best-effort: if the recreate itself fails (e.g. a transient Supabase error), resolution still succeeds and returns the plain embedded-pack record (Source `builtin_pack`) rather than failing the flow start — a mirror-sync hiccup must not block a flow the embedded pack alone is enough to run. Updated the pre-existing `TestFlowDefinitionResolverFallsBackToPackWhenMirrorMissing` (renamed to `TestFlowDefinitionResolverRecreatesMissingMirror`, since its old assertion encoded the bug itself) to verify the mirror is actually recreated and persisted (a second independent `GetByPackFlow` finds it), and added `TestFlowDefinitionResolverFallsBackToPackWhenRecreateFails` for the graceful-degradation half.

# 16. [FIXED] [P2] Edge-driven state không persist qua resume/restart. 
activeFlowEdges/activeFlowNodes chỉ nằm trên interactiveRun memory ([interactive_service.go (line 154)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:154)), nhưng ProviderSessionState chỉ persist loop/auto/cohort, không flow edges/nodes ([workflow_store.go (line 94)](C:/working/flowpilot/apps/local-runner/internal/runner/workflow_store.go:94), [workflow_store.go (line 98)](C:/working/flowpilot/apps/local-runner/internal/runner/workflow_store.go:98)). sessionStateOf cũng không snapshot chúng ([interactive_service.go (line 1199)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:1199)). Sau restart, continue routing rơi về isCoderRun fallback, phá custom/non-coder node flows.

**Fix**: confirmed real and reachable — unlike BUG#31 below, the affected run here is the top-level chat/hub run, which `loadPersistedRun`/`reconstructRun` DOES restore when a user reopens it after a restart (only child agent runs are excluded from restoration, via the `RunKind != "chat"` guard). A user reopening a chat mid-flow after a restart genuinely lost `activeFlowEdges`/`activeFlowNodes`, silently reverting edge-driven back-edge routing and forward auto-advance to legacy `isCoderRun` role matching. Added `ActiveFlowEdges`/`ActiveFlowNodes` to `ProviderSessionState` (`workflow_store.go`); `sessionStateOf` now snapshots them from `rs.activeFlowEdges`/`activeFlowNodes`, and `reconstructRun` restores them onto the rebuilt run. Also wired the local file session store's NDJSON record (`ndjsonSessionRecord`, `local_file_session_store.go`) to round-trip both fields — the Supabase-backed session store (`supabase_workflow_store.go`) has a separate, larger pre-existing gap (it doesn't persist `LoopState`/`AutoOrchestrate`/`FlowCohortID` either, none of which this bug note calls out) that's out of scope for this fix. Covered by 3 new tests: `TestSessionStateOfSnapshotsTrackedFlowTopology` (write side), `TestReconstructRunRestoresTrackedFlowTopology` (in-memory round-trip via `loadPersistedRun`), and `TestLocalFileSessionStoreActiveFlowTopologyRoundTrip` (disk persistence round-trip).

# 17. [FIXED] [P2] Auto-advance generic function vẫn bị call-site gate bằng isCoderRun. 
tryAdvanceFlowFromNode nhận arbitrary completedNodeID ([flow_executor.go (line 135)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:135)), nhưng completion path chỉ gọi khi child là isCoderRun(rs) ([interactive_service.go (line 1364)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:1364)). CP-42 có scenario non-Plan/non-Coding naming; flow có entry delegate không mang role/name coder sẽ không walk forward edges

**Fix**: confirmed real. The `EventTurnCompleted` handler's gate is now `isCoderRun(rs) || parentHasTrackedFlow(s, rs.parentRunID)` (new helper, `interactive_service.go`) — a run whose parent is a flowRef-tracked hub (`activeFlowEdges`/`activeFlowNodes` set) reaches `advanceOrNotifyHub`/`tryAdvanceFlowFromNode` regardless of its own role/agent name. `tryAdvanceFlowFromNode`'s own internal checks (tracked edges present, a forward edge exists from this specific node) still safely no-op and fall back to the same legacy note+reinvoke path for anything it doesn't recognize, so this only adds coverage — the pre-existing `isCoderRun` branch for AI-driven (non-flowRef) flows is untouched. Covered by new test `TestForwardAutoAdvanceFiresForNonCoderNamedEntryNode` in `flow_executor_test.go`, which tracks a synthetic two-node flow whose entry node is named "planner" (not "coder") and asserts the forward-edge target still auto-spawns.

# 18. [FIXED] [P1] Save/mirror steps không atomic, có thể làm mất toàn bộ steps nếu insert fail. 

SupabaseAdminRepository.saveWorkflow delete hết workflow_steps rồi mới insert lại; nếu insert lỗi FK/unique/network, workflow còn lại không có steps ([supabaseAdminRepository.ts (line 562)](C:/working/flowpilot/packages/flowpilot-client-core/src/data/supabaseAdminRepository.ts:562)). Runner mirror cũng làm tương tự trong replaceSteps: DELETE trước, POST insert sau ([supabase_workflow_flow_store.go (line 342)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:342), [supabase_workflow_flow_store.go (line 379)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:379)). Đây là user-data-loss/stale-mirror risk, nên nên chuyển sang RPC/transaction hoặc insert-new-then-swap.

**Fix**: confirmed real in both places (desktop app's `saveWorkflow`, called by `WorkflowsSettings.tsx` — in scope, unlike admin-web — and the Go mirror-sync's `replaceSteps`). Applied the suggested insert-new-then-swap approach to both, without a Postgres RPC/transaction (PostgREST has no multi-statement transaction support from a plain REST client): insert the replacement steps first, then delete only the rows not in the newly-inserted set. A failed insert now leaves the existing steps completely untouched and the caller sees an error, instead of the old rows already being gone. Go's `insertSteps` offsets new rows' `order_index` by a large constant during the insert to avoid tripping `workflow_steps`' `unique(workflow_id, order_index)` constraint against the still-live old rows sharing the same `0..N-1` range, then best-effort renormalizes it back afterward (cosmetic only — node ordering stays correct either way, since `recordFromWorkflowRow` sorts by `order_index` and all new rows share the same offset). Covered by 4 new tests: Go's `TestSupabaseWorkflowFlowStoreReplaceStepsInsertsBeforeDeleting`/`...InsertFailureLeavesNoDeleteCall` and TS's `saveWorkflow inserts new steps before deleting superseded ones`/`saveWorkflow does not delete existing steps when the insert fails` (`tests/phase1/workflowFlowEngineAttrs.test.ts`).

# 19. [FIXED] [P2] Mirror sync dùng anon key fallback dù RLS chỉ mở cho authenticated.
 FlowDefinitionStoreFor fallback sang resp.AnonKey nếu thiếu service role ([supabase_workflow_flow_store.go (line 431)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:431)), nhưng policies cho workflows/workflow_steps là to authenticated ([20260522101000...sql (line 11)](C:/working/flowpilot/supabase/migrations/20260522101000_open_workflow_engine_rls_for_admin.sql:11), [20260522101000...sql (line 21)](C:/working/flowpilot/supabase/migrations/20260522101000_open_workflow_engine_rls_for_admin.sql:21)). Runner calling PostgREST with bare anon key is role anon, not a user JWT, so writes will fail. Config probe even treats service role as required ([supabase_config.go (line 276)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_config.go:276)), so the fallback is misleading.

**Fix**: confirmed real. `FlowDefinitionStoreFor` (`supabase_workflow_flow_store.go`) no longer falls back to `resp.AnonKey` — it requires a real service role key, matching `SaveSupabaseWorkspaceConfig`'s own existing requirement (a properly-configured workspace always has one; the fallback was already unreachable in the normal flow, just misleading if ever hit). Updated the pre-existing `TestFlowDefinitionStoreForConfiguredRunnerYieldsSupabaseStore` (which had encoded the old anon-key-only expectation) to seed a service role key via the injectable secret store, and added new test `TestFlowDefinitionStoreForAnonKeyOnlyYieldsNil` asserting an anon-key-only config now correctly yields no store.

# 20. [FIXED] [P2] “Built-in read-only” chỉ enforce ở app code, DB policy vẫn cho sửa. 
Migration thêm editable=false semantics, but RLS allows all authenticated writes with with check (true) ([20260522101000...sql (line 12)](C:/working/flowpilot/supabase/migrations/20260522101000_open_workflow_engine_rls_for_admin.sql:12), [20260522101000...sql (line 22)](C:/working/flowpilot/supabase/migrations/20260522101000_open_workflow_engine_rls_for_admin.sql:22)). UpdateUserFlow/saveWorkflow guard at code level ([flow_definition_resolver.go (line 212)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_definition_resolver.go:212), [supabaseAdminRepository.ts (line 535)](C:/working/flowpilot/packages/flowpilot-client-core/src/data/supabaseAdminRepository.ts:535)), but a direct Supabase update can mutate mirrored built-ins, which CP-42 explicitly lists as a failure case.

**Fix**: confirmed real. Added a new migration, `20260701100000_restrict_builtin_workflow_writes_at_rls.sql` (the existing RLS migration is already applied, so amended via a new migration rather than editing it in place). Splits the `authenticated write workflows`/`workflow_steps` `for all` policies into separate INSERT/UPDATE/DELETE policies gated on `is_builtin = false` (`workflow_steps` has no `is_builtin` column of its own — gated via an `exists` subquery against its parent `workflows` row). DELETE gets its own explicit `using` clause, since a bare `with check` never applies to DELETE and would otherwise leave built-in rows deletable. Mirror-sync is unaffected: as of #19's fix it always authenticates with the service role key, and Postgres/Supabase's `service_role` always bypasses RLS entirely by design — this migration only closes the gap for a direct client-side (authenticated-role) mutation. Not applied to any live database from this environment — creating the migration file is the appropriate scope here, matching how the CP-42 migration itself was only ever a file until deliberately deployed. Covered by new test `TestBuiltinWorkflowWritesRestrictedAtRLS` (`workflow_flow_migration_sql_test.go`), asserting the migration text directly: all six policies exist, each references `is_builtin`, and both DELETE policies have their own `using` clause.

# 21. [FIXED] [P2] Supabase mirror normalized shape không identical với embedded pack.
 Embedded path keeps FlowDefinition.ID = def.ID via builtinRecordFromFlow ([flow_definition_resolver.go (line 160)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_definition_resolver.go:160)), but Supabase row reconstruction sets Definition.ID to the UUID row id ([supabase_workflow_flow_store.go (line 154)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:154)). Task-175 says resolver should return the same normalized shape for mirror/custom; any downstream that keys on Definition.ID gets "review-loop" in fallback but UUID when mirror exists.

**Fix**: confirmed real (a latent shape-inconsistency bug — nothing reads `record.Definition.ID` at runtime yet, confirmed via search, so no active feature is broken today, but the two sources genuinely disagreed and any future code keying on it would silently branch on the flow's source). `recordFromWorkflowRow` (`supabase_workflow_flow_store.go`) now uses `row.PackFlowID` (the pack's own semantic id, e.g. `"review-loop"`) as `Definition.ID` when the row is a mirrored built-in, matching `builtinRecordFromFlow`'s embedded-pack shape exactly; falls back to the row's UUID only for a genuine user-owned flow, which has no other semantic id. Covered by a new assertion added to the existing `TestSupabaseWorkflowFlowStoreGetByRefBuiltinPackFlowID` test.

# 22. [FIXED] [P3] Pack has two sources of flow metadata and no drift validation.
 Manifest carries selectableIn/chatSubModes/cloneable ([manifest.yaml (line 11)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/manifest.yaml:11)), flow YAML carries builtin.* too ([review-loop.yaml (line 5)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/review-loop.yaml:5)), but picker/mirror use def.Builtin from the flow file ([chat_builtin_orchestration.go (line 42)](C:/working/flowpilot/apps/local-runner/internal/runner/chat_builtin_orchestration.go:42)) while ValidateManifestFS only checks manifest refs/basic values ([pack.go (line 324)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:324)). A future manifest-only metadata edit would silently do nothing.

**Fix**: confirmed real. Added `validateManifestFlowMatchesDefinition` (`pack.go`), called from `LoadPackFS` right after each flow file is loaded — where both the manifest entry and the parsed `FlowDefinition` are available. Compares `editable`/`chatBaseline`/`cloneable` (exact bool match) and `selectableIn`/`chatSubModes` (order-independent set match, via a new `stringSetEqual` helper) between the two sources, failing the pack load on any disagreement. Confirmed the real embedded pack's manifest and both flow files already agree (no drift exists today) — `go test ./internal/agentpack/...` stays green with this stricter check in place. Covered by new test `TestLoadPackFSRejectsManifestFlowMetadataDrift`, using a fixture where the manifest declares `selectableIn: [chat]` but the flow file declares `builtin.selectableIn: [flow]`.

# 23. [FIXED] P1 - Built-in flow pack có thể chạy nhầm agent local thay vì agent trong pack.

review-loop.yaml trỏ agent: agents/coder.md / agents/reviewer.md, nhưng runtime rút gọn path thành basename qua flowNodeAgentName() rồi gọi catalog bằng coder/reviewer. Catalog lại ưu tiên project-local/provider-home trước built-in, nên nếu repo có .codex/agents/reviewer.md hoặc .claude/agents/reviewer.md, built-in Review Loop sẽ âm thầm dùng prompt khác pack. Điều này phá claim pack-driven ở CP-42/CA-158.

Refs: [review-loop.yaml (line 32)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/review-loop.yaml:32), [flow_executor.go (line 243)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:243), [agent_catalog.go (line 54)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_catalog.go:54), [agent_catalog.go (line 78)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_catalog.go:78)

**Fix**: confirmed real — traced the full path: `AgentCatalog.listAgents` precedence is project-local > provider-home > built-in, and pack agents (`loadBuiltinAgentDefinitionsFromPack`) are merged in at the lowest tier, so any repo defining its own `.claude/agents/coder.md`/`.codex/agents/coder.md` (for any unrelated purpose) would silently win over the pack's own bundled coder agent for every flow-executor spawn. Added `SpawnAgentInput.AgentDefOverride` (internal-only, `json:"-"`, mirroring the existing `UIInitiated` pattern) and a new `resolvePackAgentDefinition(agentName)` helper (`flow_executor.go`) that looks up the pack's own bundled agent definitions directly, bypassing `AgentCatalog.listAgents` entirely. All three flow_executor.go spawn call sites (`startResolvedFlow`'s entry loop, `startInlineEntryChain`, `tryAdvanceFlowFromNode`'s cohort loop) now resolve and pass this override; `spawnChildRun` uses it in place of the name-based catalog lookup when set. A resolution failure (pack agent not found) falls back to the pre-existing catalog-based lookup rather than blocking the spawn. AI-driven `spawn_agent` tool calls are unaffected — they never set this field, so a project-local/provider-home agent remains legitimately reachable by name for non-flow-executor spawns. Covered by new test `TestStartResolvedFlowSpawnsPackAgentEvenWhenProjectShadowsItsName` in `flow_executor_test.go`, which writes a `.claude/agents/coder.md` shadow agent with `role: hijacked` into a temp workspace and asserts the spawned coder child still carries the pack's own `role: coder`.

# 24. [FIXED] P1 - submit_review_outcome vẫn được expose cho normal chat.

Codex luôn thêm codexReviewOutcomeDynamicTool() vào dynamicTools, Claude tools/list cũng luôn trả submit_review_outcome. CP-36 manual expected ghi rõ normal chat không được offer tool này. Tệ hơn, nếu model gọi nhầm, SubmitFlowControl đi thẳng vào applyFlowControl, chỉ check run tồn tại rồi mutateLoop() tạo/mutate loop state cho cả run thường.

Refs: [codex_adapter.go (line 151)](C:/working/flowpilot/apps/local-runner/internal/runner/codex_adapter.go:151), [claude_mcp_server.go (line 305)](C:/working/flowpilot/apps/local-runner/internal/runner/claude_mcp_server.go:305), [interactive_service.go (line 471)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:471), [CP-36 (line 380)](C:/working/flowpilot/requirements/07-Coding-Plan/done/CP-36-Agent-Review-Loop-And-Main-Hub-Orchestration.md:380)

**Fix**: confirmed real on both providers. Added `TurnRequest.OfferReviewOutcomeTool`, set in `runTurn` from `rs.autoOrchestrate` — true only for a run actually acting as a flow hub (set when `startResolvedFlow`/an AI `spawn_agent(autoOrchestrate: true)` call starts a flow), false for plain `normal_chat` and for spawned reviewer/coder children. Codex's `SendTurn` now only appends `codexReviewOutcomeDynamicTool()` to `dynamicTools` when the flag is true; `handleDynamicToolCall` additionally re-checks a per-thread `allowReviewOutcome` map before acting on a call (defense in depth against a model calling a tool it was never shown, e.g. from stale resumed-session context). Claude's MCP server mirrors this: `register(bridge, allowReviewOutcome)` now takes the flag, `tools/list` omits `submit_review_outcome` from `claudeMCPToolDefs` when false, and `tools/call` re-checks the same per-token flag before dispatching. Covered by 4 new tests: Codex's `TestCodexAdapterDoesNotAdvertiseReviewOutcomeToolByDefault`/`...RejectsReviewOutcomeCallWhenNotOffered`, and Claude's `TestClaudeMCPToolsListIncludesReviewOutcomeOnlyWhenAllowed`/`...SubmitReviewOutcomeRejectedWhenNotAllowed`.

# 25. [FIXED] P2 - Pack YAML path không reject duplicate continue back-edge, runtime chọn edge đầu tiên.

resolveContinueBackEdgeTarget() comment nói nếu nhiều back-edge continue thì trả edge đầu tiên vì live path không disambiguate. Có helper validateFlowEdges() reject duplicate back-edge, nhưng pack parser dùng validateFlowDefinition() riêng và chỉ check endpoint/kind, không check duplicate (kind=back, when=continue). Custom flow có 2 loop path sẽ route sai mà không fail-fast.

Refs: [flow_executor.go (line 77)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:77), [pack.go (line 641)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:641), [agent_orchestrator.go (line 578)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:578)

**Fix**: confirmed real — `agent_orchestrator.go`'s `validateFlowEdges` is only ever called from its own tests, never from the actual pack-loading path (and `internal/agentpack` can't import `internal/runner` to reuse it — that would be a cycle). Added the same duplicate-back-edge check directly inside `agentpack.validateFlowDefinition` (`pack.go`): a `backEdgeSources map[when]from` tracks the first-seen source for each `(kind=back, when=X)` combination and rejects a second one. Covered by new test `TestValidateFlowDefinitionRejectsDuplicateContinueBackEdge` (`pack_test.go`).

# 26. [FIXED] P2 - Generic flow vẫn nhận prompt review/coder-specific.

flow-start-wait.md nói “coder agent” và “review coordinator”; coder-reentry.md nói “Review completed… resubmit the code”; fallback trong Go cũng hardcode submit_review_outcome. Với rag-harness.yaml có back-edge validate -> implement, một continue sẽ reinvoke node đúng hơn trước nhưng prompt vẫn là review-loop/coder language, dễ làm non-review flow đi sai ý định.

Refs: [flow-start-wait.md (line 1)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/prompts/flow-start-wait.md:1), [coder-reentry.md (line 1)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/prompts/coder-reentry.md:1), [interactive_service.go (line 613)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:613), [rag-harness.yaml (line 64)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:64)

**Fix**: confirmed real. Reworded both pack prompts to flow-agnostic language: `flow-start-wait.md` now says "An agent has already been spawned..." instead of "A coder agent"/"review coordinator"/"review cycle"; `coder-reentry.md` now says "Feedback received on your last submission. Address every issue below and resubmit your work." instead of "Review completed... resubmit the code". `auto-reinvoke.md` was already domain-free (confirmed by reading it — it says "call the declared control tool for this flow when one is available", only naming `submit_review_outcome` as a review-loop-specific example) and needed no change. Updated every Go fallback string that mirrors these prompts (used only when the pack file can't be loaded) to match: `notifyHubFlowStarted`'s and `startTurn`'s wait-notice fallbacks (`flow_executor.go`, `interactive_service.go`), `autoReinvokePromptText`'s fallback, `buildCoderReentryPrompt`'s empty-content fallback, and `maybeReinvokeCoderForContinue`'s empty-prompt fallback (all `interactive_service.go`) no longer hardcode "coder"/"review". Updated the pre-existing tests that had encoded the old domain-specific wording as their expected content (`TestLoadBuiltinCoderReentryPrompt`, `TestStartResolvedFlowNotifiesHubToWait`, `TestStartTurnWithFlowRefPrependsWaitNoticeToHubsOwnFirstTurn`), and added new test `TestFlowStartWaitPromptIsDomainFree` asserting the prompt contains none of "coder"/"review coordinator"/"review cycle".

# 27. [FIXED] P2 - command.validate có thể coi command fail là pass khi args đi qua JSON/tool payload.

[behavior_registry_builtin.go (line 128)](C:/working/flowpilot/apps/local-runner/internal/runner/behavior_registry_builtin.go:128) assert exitCode là int, nhưng JSON decode vào map[string]any thường ra float64. Vì vậy {"exitCode":1} có thể rơi về default 0 và trả done / command validation passed. Test hiện tại chỉ dùng Go int, nên không bắt được case runtime này: [behavior_registry_test.go (line 187)](C:/working/flowpilot/apps/local-runner/internal/runner/behavior_registry_test.go:187). rag-harness dùng behavior này ở [rag-harness.yaml (line 40)](C:/working/flowpilot/apps/local-runner/internal/agentpack/flow-pack/flows/rag-harness.yaml:40).

**Fix**: confirmed real. Added `toIntArg(v any) (int, bool)` (`behavior_registry_builtin.go`), coercing `int`/`int32`/`int64`/`float32`/`float64` — `behaviorCommandValidate` now uses it instead of a bare `.(int)` assertion. Covered by new test `TestBehaviorCommandValidateHandlesJSONDecodedFloat64ExitCode`, which round-trips `{"exitCode":1}` through `json.Unmarshal` (confirming it decodes as `float64`, not `int`) and asserts the behavior correctly reports `continue`, not `done`.

# 28. [FIXED] P2 - Workflow load từ Supabase không validate behavior_id, nên flow lỗi có thể “im lặng” không chạy node.
YAML pack có validate unknown behavior ở [pack.go (line 631)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:631), nhưng Supabase path chỉ copy raw behavior_id vào node ở [supabase_workflow_flow_store.go (line 180)](C:/working/flowpilot/apps/local-runner/internal/runner/supabase_workflow_flow_store.go:180) rồi executor skip unknown behavior ở [flow_executor.go (line 157)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:157) và entry node cũng skip ở [flow_executor.go (line 234)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:234). Settings còn cho nhập Behavior ID tự do ở [WorkflowsSettings.tsx (line 770)](C:/working/flowpilot/apps/desktop-flowpilot/src/components/settings/WorkflowsSettings.tsx:770). Kết quả: user/DB workflow sai schema có thể resolve thành công nhưng không spawn node, thay vì fail-fast rõ ràng như CP-42 mong muốn.

**Fix**: confirmed real. Exported `agentpack.validateFlowDefinition` as `agentpack.ValidateFlowDefinition` (only one production caller, `LoadFlowFS`, updated) so it's usable outside the package. `FlowDefinitionResolver.ResolveFlowRef`/`ResolveBuiltin` (`flow_definition_resolver.go`) now call it on every record returned from the store, for both the `GetByRef` and `GetByPackFlow` lookups — a Supabase row with an unknown `behavior_id` now fails resolution with a clear error instead of silently resolving and then spawning nothing. The embedded-pack path is unaffected (already validated at `LoadFlowFS` time; running the same check again is harmless). Covered by new test `TestFlowDefinitionResolverRejectsStoredDefinitionWithUnknownBehavior` in `flow_definition_resolver_test.go`.

# 29. [FIXED] P3 - Chat Intent / built-in orchestration picker vẫn editable sau turn đầu, nhưng runner chỉ honor ở first turn.
UI chỉ disable khi status là running: [ChatWorkspace.tsx (line 176)](C:/working/flowpilot/apps/desktop-flowpilot/src/components/ChatWorkspace.tsx:176), nên sau khi turn đầu kết thúc user vẫn đổi Task/Bug/flowRef được. Nhưng client chỉ gửi changeType/subMode/flowRef khi !runId: [store.ts (line 971)](C:/working/flowpilot/apps/desktop-flowpilot/src/state/store.ts:971), [store.ts (line 1038)](C:/working/flowpilot/apps/desktop-flowpilot/src/state/store.ts:1038). Runner cũng chỉ xử lý nếu turnCount == 0: [interactive_service.go (line 2549)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service.go:2549). Đây là UX-contract drift: UI nhận thay đổi nhưng backend sẽ ignore.

**Fix**: confirmed real, though more precisely a UI-only drift than a wire-level one — the client's own `sendMessage` already correctly gates `changeType`/`sourceDocId`/`subMode`/`flowRef` on `isFirstChatTurn = chatMode === "normal_chat" && !runId`, so nothing wrong is ever actually sent to the runner after turn 1. The bug is that `ChatStartIntentPanel` only disabled the picker while `runStatus === "running"`, so it became clickable again the moment turn 1 completed — misleadingly suggesting to the user that changing Task/Bug/flowRef mid-conversation does something, when the client has already permanently stopped transmitting it for the life of that `runId`. Added a `chatStarted = Boolean(runId)` check and disabled the picker whenever `runStatus === "running" || chatStarted`, with updated copy ("Locked after the first message — start a new chat to change the intent.") explaining why. Verified via `tsc` against the real project config (`apps/desktop-flowpilot/tsconfig.json`, which — unlike `tsconfig.phase1-tests.json` used for earlier fixes in this pass — actually includes `.tsx` files including `ChatWorkspace.tsx`/`WorkflowsSettings.tsx`; confirmed via `--listFiles`): clean compile, no errors.

# 30. [FIXED] P3 - validation.summarize cũng type-fragile giống trên.
[behavior_registry_builtin.go (line 143)](C:/working/flowpilot/apps/local-runner/internal/runner/behavior_registry_builtin.go:143) chỉ nhận issues kiểu []any; nếu caller nội bộ truyền []string hoặc typed issue slice thì nó xem như empty và trả done. Test cũng chỉ cover []any: [behavior_registry_test.go (line 176)](C:/working/flowpilot/apps/local-runner/internal/runner/behavior_registry_test.go:176).

**Fix**: confirmed real. Added `issuesLen(v any) int` (`behavior_registry_builtin.go`), handling `[]any`/`[]string`/`[]ReviewIssue` directly and falling back to `reflect.ValueOf(v).Len()` for any other slice/array shape. `behaviorValidationSummarize` now uses it instead of a bare `.([]any)` assertion. Covered by new test `TestBehaviorValidationSummarizeCountsNonAnySliceIssues`, which passes a `[]string` issues list and asserts the behavior correctly reports `continue`, not `done`.

# 31 / P2 - [VERIFIED — NOT REPRODUCIBLE TODAY] Cohort barrier mất expected count sau restart, loop có thể kẹt vĩnh viễn.
Session store chỉ persist FlowCohortID, không persist CohortSize/expected count: [workflow_store.go (line 101)](C:/working/flowpilot/apps/local-runner/internal/runner/workflow_store.go:101), [local_file_session_store.go (line 76)](C:/working/flowpilot/apps/local-runner/internal/runner/local_file_session_store.go:76). Khi child hoàn tất, cohortComplete yêu cầu cohortExpected > 0: [agent_orchestrator.go (line 94)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:94). Sau reconstruct chỉ restore flowCohortId: [interactive_resume.go (line 385)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_resume.go:385), không rebuild cohortExpected, nên reviewer cohort hoàn tất sau restart sẽ append result nhưng không bao giờ join/reinvoke hub.

**Investigated, no code change**: the specific `cohortExpected` persistence gap is real, but traced the actual mechanism the note describes ("a reviewer cohort completes after restart") and could not reproduce it as reachable in the current architecture. `reconstructRun` — the only function that ever rebuilds an `interactiveRun` from persisted `ProviderSessionState` — is called exclusively from `loadPersistedRun`, which explicitly rejects any session whose `RunKind != "chat"` ("only chat runs can be resumed in this version"). Child agent runs (coder/reviewer/cohort members) are never persisted with `RunKind == "chat"` — only the top-level hub run is. So after a restart, a child agent run is **never** reconstructed as a live `interactiveRun` at all; there is no code path by which a cohort member could "complete after restart" and call `appendCohortResult`, because nothing ever brings it back to a state where it could run a turn and emit `EventTurnCompleted` in the first place. The underlying provider CLI subprocess a child was driving is also not designed to survive/reattach across a runner process restart (no persistent out-of-process bridge exists for that). Concretely: `cohortExpected` not surviving a restart is real, but it is dead code today — nothing can ever read a stale-vs-fresh `cohortExpected` value for a cohort whose members can't be resurrected to complete in the first place. Left as a documented, low-priority latent risk rather than adding speculative persistence plumbing for a scenario this codebase's current design cannot reach — should a future feature add child-run resumption, this note (and the `ActiveFlowEdges`/`ActiveFlowNodes` restoration added for BUG#16) would be the first things to revisit.

# 32 / P2 - [FIXED] /flow-control HTTP handler reject canonical submit_review_outcome body dùng status.

Go schema chính thức của ReviewOutcomeInput là json:"status" với approved|changes_requested|blocked: [agent_orchestrator.go (line 621)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:621). Parser còn document outcome chỉ là alias: [agent_orchestrator.go (line 642)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:642). Nhưng HTTP handler chỉ gọi parseReviewOutcomeInput khi body có key outcome; body { "status": "approved" } bị đưa vào raw FlowControlInput parser và fail vì raw status chỉ là continue|done|escalate: [interactive_handlers.go (line 978)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_handlers.go:978), [agent_orchestrator.go (line 472)](C:/working/flowpilot/apps/local-runner/internal/runner/agent_orchestrator.go:472). Test hiện chỉ cover alias outcome: [interactive_service_test.go (line 1698)](C:/working/flowpilot/apps/local-runner/internal/runner/interactive_service_test.go:1698).

**Fix**: confirmed real — `parseReviewOutcomeInput` already correctly accepted both `"status"` and `"outcome"` keys, but `handleSubmitFlowControl`'s routing decision only checked for the literal `"outcome"` key's presence, so a canonical `{"status":"approved"}` body never reached that parser at all. Changed the routing condition to `hasOutcome || reviewOutcomeStatuses[statusVal]` — routes to the `ReviewOutcomeInput` path whenever either the legacy `"outcome"` key is present or `body["status"]` is one of the review-outcome values (`approved`/`changes_requested`/`blocked`), which never collide with the raw `FlowControlInput` values (`continue`/`done`/`escalate`). Covered by new test `TestSubmitFlowControlHTTPAcceptsCanonicalStatusField`, which POSTs `{"status":"approved"}` and asserts it succeeds and maps to `done`, same as the existing `outcome`-alias test.

# 33 / P3 - [FIXED] Pack validator không validate dependsOn references.
dependsOn được parse vào node: [pack.go (line 607)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:607), migration comment nói nó reference node_id: [20260701090000_add_flow_engine_attrs_to_workflows.sql (line 48)](C:/working/flowpilot/supabase/migrations/20260701090000_add_flow_engine_attrs_to_workflows.sql:48). Nhưng validateFlowDefinition chỉ validate node IDs và edge endpoints, không check từng dependsOn trỏ tới node tồn tại: [pack.go (line 614)](C:/working/flowpilot/apps/local-runner/internal/agentpack/pack.go:614). Schema typo có thể pass load/mirror thay vì fail-fast.

**Fix**: confirmed real. `validateFlowDefinition` (`pack.go`) now walks every node's `DependsOn` entries after building the node-ID set, rejecting any entry that doesn't match a real node ID in the same flow. Covered by new test `TestValidateFlowDefinitionRejectsMissingDependsOnReference` (`pack_test.go`), using a typo'd `dependsOn: ["cotnext"]`.

# 34 / P3 - [FIXED] Change-audit CA-160/CA-161 đã stale so với code hiện tại.
CA-161 vẫn nói forward progression coder -> reviewers -> synthesis là AI-driven, không edge-driven: [CA-161 (line 32)](C:/working/flowpilot/change-audit/CA-161-edge-driven-continue-reinvoke.md:32). Nhưng code đã có tryAdvanceFlowFromNode auto-spawn reviewer cohort từ edges: [flow_executor.go (line 113)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor.go:113), và test xác nhận: [flow_executor_test.go (line 97)](C:/working/flowpilot/apps/local-runner/internal/runner/flow_executor_test.go:97). CA-160 cũng nói edges_json chưa được read bởi live executor: [CA-160 (line 15)](C:/working/flowpilot/change-audit/CA-160-flow-definitions-migrated-to-workflows-table.md:15). Ledger này sẽ làm review sau hiểu sai trạng thái CP-42.

**Fix**: confirmed real. Added [CA-163](/C:/working/flowpilot/change-audit/CA-163-forward-edge-auto-spawn-reviewer-cohort.md) documenting the forward-edge auto-spawn feature (which previously had no CA note of its own at all). CA-161's "Still open" section amended to point to CA-163 instead of restating the now-false "forward progression is AI-driven" claim. CA-160's `edges_json` line amended to note it is now read by the live executor.