Our system MVP is for personal use

We hard code in .env file some supabase attrs
SUPABASE_API_URL=Value
SUPABASE_API_KEY=Value
SUPABASE_API_EDGE_FUNCTION_URL=Value
SUPABASE_SERVICE_ROLE_KEY=Value

But after the MVP done we tend to introduce/share this tool for others

i Think we can have one page Called Env setting where we can push above values base on our supabase project

So let say, i can create my supbase project then input it data to this tool
Then i can use this tool in my scoped-data

WE can introduce this page in Menu as well as before Login page
