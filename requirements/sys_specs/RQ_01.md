1. User can create projects
2. Allow user select AI type: support gemini. claude and codex now
2. User can add MCP servers as context sources to projects
   - Git MCP
   - Jira MCP
   - Figma MCP
   - Driver MCP

4. Allow users to create/run flows in each project
Flows is including many steps. the full flow is
 -> Business Idea
-> Feature Intake
→ Business Summary
→ Product Spec
→ Android Tech Spec
-> Make plan coding
-> create architecture
-> create unit test TDD with the signature only
→ Task Breakdown
→ Implementation Plan→ Master Schedule / Resource-based Task Assignment
→ Code/Review Loop
→ CI Validation
→ Release Readiness
→ Runtime Feedback
→ Future Planning

We allow user to run each step manually.
Or we allow they create custom flow from the above existing steps.
That mean we need to have to enable/disable step and also re-oder steps

For example i can create a short flow: BUG_FIX_FLOW, it include steps: 
- make plan coding 
- create architecture 
- create unit test TDD with the signature only
- Code/Review Loop

Or i can create a long flow: NEW_FEATURE_FLOW, it include steps: 
- Business Idea
- Feature Intake
- Business Summary
- Product Spec
- Android Tech Spec
- Make plan coding
- create architecture
- create unit test TDD with the signature only
- Code/Review Loop
- CI Validation
- Release Readiness
- Runtime Feedback
- Future Planning 

We can pre define some basic flow (but not all). Allow user to duplicate and modify to create their own flow

5. Each step in flow can be configured
- Enable/Disable WAITING USER approval gate or yolo mode from AI
- Each step can belong with 1-n SKILL, 1-n AGENT
- Each step can has a required MCP. for example, a step: 'Make plan coding' needs Jira MCP to get the ticket
- Each step can provide output artifacts that can be used as context for other steps
- We need to save these artifacts, can view as history and also allow to backup/sync: support sync to DriverMCP or local pc

6. SKILL
- Skill can be added to each step in workflow
- 2 types: Built-in skill from system (coding, architecture, planning, testing...), custom skill created by user
- Each skill can be executed with or without human approval

Build-in skills are our mandatory skills.
for example, the Step plan in workflow must include: Planning skill 
User can crate/add more skill to this step but they can not delete built-in skill

6.1 Where can we save skills?
WE can save it in current project folder (let check nextjs can do it or not)

And then , cause we have 3 AI type to support
Then we need to save skill to .agents, .claude and .codex

6.2 for the custom skill
We allow user to create custom skill in each project. but we also allow user to sync/backup custom skill to DriverMCP
And user can also export custom skill to use in other projects. 


7. AGENT
Now allow build-in agent only

each step has 1 agent if needed. for exmaple COding step need coder-agent to run in separate process

When step has agent -> must provide it to golang to correctly to call go-runner tool
